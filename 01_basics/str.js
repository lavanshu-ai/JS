let str='lavanshu patidar'
const surname=str.slice(9,16)
console.log(surname)
const name=str.substring(0,8)
const trial=str.slice(-7)
const trial=str.slice(-7,-1)
//slice and substring are used to extract parts of a string
//slice can take negative indices, substring cannot
console.log(trial)
console.log(name)

